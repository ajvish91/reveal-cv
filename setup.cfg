# Canonical packaging metadata lives in pyproject.toml.
# Keep this file aligned for callers that still invoke setup.cfg / setuptools legacy paths.
[metadata]
name = job-search-automation
version = 0.1.0

[options]
packages = find:
py_modules = repo_paths
python_requires = >=3.10
install_requires =
    pyyaml>=6.0.1
    pypdf>=4.0.0
    certifi>=2024.0.0
    selectolax>=0.3.21
    streamlit>=1.32.0
    pandas>=2.0.0

[options.packages.find]
where = .
include = shared*, job_search*, cv_generation*

[options.entry_points]
console_scripts =
    ingest-nav-jobs = job_search.ingest_nav_jobs:main
    ingest-finn-jobs = job_search.ingest_finn_jobs:main
    score-jobs = job_search.score_jobs:main
    job-dashboard = job_search.dashboard:main
    run-cv-tailoring = cv_generation.run_cv_tailoring:main
